# Simple Python App

This is a simple Python application with a hello function.

## Usage

Run the app: `python app/main.py`

Run tests: `python -m pytest tests/`

## Requirements

Install dependencies: `pip install -r requirements.txt`